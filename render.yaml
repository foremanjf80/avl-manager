# Render blueprint. The disk is the whole point: without it, Render's filesystem
# is ephemeral and avl.db plus every uploaded document is lost on each deploy.
services:
  - type: web
    name: avl-manager
    runtime: python
    plan: starter                 # paid: a persistent disk is not available on free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    disk:
      name: avl-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.3"
      - key: AVL_DB                # database on the persistent disk
        value: /data/avl.db
      - key: AVL_UPLOADS           # uploaded documents beside it
        value: /data/data_uploads
      - key: SECURE_COOKIES
        value: "1"                 # Render terminates TLS for you
      - key: ALLOWED_DOMAIN
        value: qcells.com
      - key: SECRET_KEY
        generateValue: true        # Render generates and stores it
      # Set in the dashboard, never in Git. Use oidc if you can register an
      # Entra app; shared + SHARED_PASSWORD is the stopgap until then. Leaving
      # this as dev on a public URL means no password at all.
      - key: AUTH_MODE
        sync: false
      - key: SHARED_PASSWORD
        sync: false
      - key: OIDC_CLIENT_ID
        sync: false
      - key: OIDC_CLIENT_SECRET
        sync: false
      - key: OIDC_METADATA_URL
        sync: false
