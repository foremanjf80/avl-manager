# Copy to .env.backup (gitignored) so the token is not in your scheduler entry.
AVL_URL=https://avl-manager.onrender.com
AVL_BACKUP_TOKEN=<the BACKUP_TOKEN set on the server>
# Where to keep them. A synced folder gets them off this machine too.
AVL_BACKUP_DIR=/mnt/c/Users/john.foreman/OneDrive/avl-backups
AVL_BACKUP_KEEP=30
